{
  "name": "@velocity/framework",
  "version": "0.1.0",
  "description": "A minimal, fast TypeScript framework with decorators, built-in ORM, and zero bloat.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "sync": "bun scripts/sync.js",
    "dev": "bun run build && bun run sync",
    "velogen": "bun scripts/velogen.js",
    "demo": "cd examples/full-demo && bun run main.ts",
    "test": "bun test tests/",
    "test:demo": "bun run build && bun test examples/full-demo/tests/",
    "test:all": "bun run build && bun test tests/ examples/full-demo/tests/"
  },
  "keywords": ["typescript", "framework", "orm", "decorators", "minimal"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/bun": "^1.3.13",
    "joi": "^18.0.1",
    "mysql2": "^3.14.3",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/pg": "^8.15.5",
    "bun-types": "latest",
    "typescript": "^5.9.2"
  }
}
