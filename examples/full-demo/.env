# App
APP_NAME=velocity-demo
APP_DEBUG=true

# Server
SERVER__HOST=0.0.0.0
SERVER__PORT=5000

# Auth
AUTH__JWT_SECRET=my-secret-key
AUTH__TOKEN_EXPIRY=3600

# SQLite database (db — users)
DB__DATABASE=:persistent:
DB__FILENAME=db.sqlite
DB__TYPE=sqlite

# PostgreSQL database (pgDb — posts, job_records)
PG__DATABASE=velocity_demo
PG__HOST=localhost
PG__PASSWORD=whathefuck3ver
PG__POOL__MAX=10
PG__POOL__MIN=2
PG__PORT=5432
PG__USER=johndoe
